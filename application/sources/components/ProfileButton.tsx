// Le bouton présent sur toutes les pages de l'application qui mêne vers la page profile.

import {View} from "react-native";

const ProfileButton = () => {
    return (
        <View
            style={{

            }}
        >

        </View>
    )
}

export default ProfileButton;